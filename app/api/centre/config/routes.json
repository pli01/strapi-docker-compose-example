{
  "routes": [
    {
      "method": "GET",
      "path": "/centres",
      "handler": "centre.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/centres/count",
      "handler": "centre.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/centres/:id",
      "handler": "centre.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/centres",
      "handler": "centre.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/centres/:id",
      "handler": "centre.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/centres/:id",
      "handler": "centre.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
